# PostgreSQL connection — matches order-db in docker-compose.yml (added when order is built)
DATABASE_URL="postgresql://order:order@localhost:6005/order_db"

# Must match the JWT_ACCESS_SECRET in microservice-auth
JWT_ACCESS_SECRET="change-me-access-secret"
