# PostgreSQL connection — matches inventory-db in docker-compose.yml
DATABASE_URL="postgresql://inventory:inventory@localhost:6004/inventory_db"

# Must match the JWT_ACCESS_SECRET in microservice-auth
JWT_ACCESS_SECRET="change-me-access-secret"
