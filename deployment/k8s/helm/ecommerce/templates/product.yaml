apiVersion: apps/v1
kind: Deployment
metadata:
  name: product
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: product
spec:
  replicas: {{ .Values.product.replicas }}
  selector:
    matchLabels:
      app: product
  template:
    metadata:
      labels:
        app: product
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      containers:
        - name: product
          image: {{ .Values.global.registry }}/ecommerce-product:{{ .Values.global.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 4002
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: PRODUCT_DATABASE_URL
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: JWT_ACCESS_SECRET
          resources:
            requests:
              cpu:    {{ .Values.product.resources.requests.cpu }}
              memory: {{ .Values.product.resources.requests.memory }}
            limits:
              cpu:    {{ .Values.product.resources.limits.cpu }}
              memory: {{ .Values.product.resources.limits.memory }}
          livenessProbe:
            tcpSocket:
              port: 4002
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 4002
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: microservice-product
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: product
  ports:
    - port: 4002
      targetPort: 4002
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.product.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: product-db
spec:
  serviceName: {{ .Values.product.db.name }}
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.product.db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.product.db.name }}
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.product.db.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.product.db.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.product.db.database | quote }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.product.db.user | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.product.db.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.product.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.product.db.name }}
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet DNS resolution
