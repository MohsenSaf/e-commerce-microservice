apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: order
spec:
  replicas: {{ .Values.order.replicas }}
  selector:
    matchLabels:
      app: order
  template:
    metadata:
      labels:
        app: order
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      containers:
        - name: order
          image: {{ .Values.global.registry }}/ecommerce-order:{{ .Values.global.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 4005
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: ORDER_DATABASE_URL
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: JWT_ACCESS_SECRET
          resources:
            requests:
              cpu:    {{ .Values.order.resources.requests.cpu }}
              memory: {{ .Values.order.resources.requests.memory }}
            limits:
              cpu:    {{ .Values.order.resources.limits.cpu }}
              memory: {{ .Values.order.resources.limits.memory }}
          livenessProbe:
            tcpSocket:
              port: 4005
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 4005
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: microservice-order
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: order
  ports:
    - port: 4005
      targetPort: 4005
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.order.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: order-db
spec:
  serviceName: {{ .Values.order.db.name }}
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.order.db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.order.db.name }}
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.order.db.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.order.db.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.order.db.database | quote }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.order.db.user | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.order.db.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.order.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.order.db.name }}
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet DNS resolution
