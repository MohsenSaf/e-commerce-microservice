apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: inventory
spec:
  replicas: {{ .Values.inventory.replicas }}
  selector:
    matchLabels:
      app: inventory
  template:
    metadata:
      labels:
        app: inventory
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      containers:
        - name: inventory
          image: {{ .Values.global.registry }}/ecommerce-inventory:{{ .Values.global.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 4004
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: INVENTORY_DATABASE_URL
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: JWT_ACCESS_SECRET
          resources:
            requests:
              cpu:    {{ .Values.inventory.resources.requests.cpu }}
              memory: {{ .Values.inventory.resources.requests.memory }}
            limits:
              cpu:    {{ .Values.inventory.resources.limits.cpu }}
              memory: {{ .Values.inventory.resources.limits.memory }}
          livenessProbe:
            tcpSocket:
              port: 4004
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 4004
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: microservice-inventory
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: inventory
  ports:
    - port: 4004
      targetPort: 4004
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.inventory.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: inventory-db
spec:
  serviceName: {{ .Values.inventory.db.name }}
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.inventory.db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.inventory.db.name }}
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.inventory.db.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.inventory.db.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.inventory.db.database | quote }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.inventory.db.user | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.inventory.db.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.inventory.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.inventory.db.name }}
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet DNS resolution
