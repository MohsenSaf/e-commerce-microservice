apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: payment
spec:
  replicas: {{ .Values.payment.replicas }}
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      containers:
        - name: payment
          image: {{ .Values.global.registry }}/ecommerce-payment:{{ .Values.global.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 4006
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: PAYMENT_DATABASE_URL
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: JWT_ACCESS_SECRET
          resources:
            requests:
              cpu:    {{ .Values.payment.resources.requests.cpu }}
              memory: {{ .Values.payment.resources.requests.memory }}
            limits:
              cpu:    {{ .Values.payment.resources.limits.cpu }}
              memory: {{ .Values.payment.resources.limits.memory }}
          livenessProbe:
            tcpSocket:
              port: 4006
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 4006
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: microservice-payment
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: payment
  ports:
    - port: 4006
      targetPort: 4006
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.payment.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: payment-db
spec:
  serviceName: {{ .Values.payment.db.name }}
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.payment.db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.payment.db.name }}
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.payment.db.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.payment.db.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.payment.db.database | quote }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.payment.db.user | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.payment.db.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.payment.db.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.payment.db.name }}
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet DNS resolution
