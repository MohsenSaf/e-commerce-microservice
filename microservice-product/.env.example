# PostgreSQL connection — matches product-db in docker-compose.yml
DATABASE_URL="postgresql://product:product@localhost:6002/product_db"

# Must match the JWT_ACCESS_SECRET in microservice-auth
JWT_ACCESS_SECRET="change-me-access-secret"
