# PostgreSQL connection — matches payment-db in docker-compose.yml (added when payment is built)
DATABASE_URL="postgresql://payment:payment@localhost:6006/payment_db"

# Must match the JWT_ACCESS_SECRET in microservice-auth
JWT_ACCESS_SECRET="change-me-access-secret"
