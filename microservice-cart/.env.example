# PostgreSQL connection — matches cart-db in docker-compose.yml (added when cart is built)
DATABASE_URL="postgresql://cart:cart@localhost:6003/cart_db"

# Must match the JWT_ACCESS_SECRET in microservice-auth
JWT_ACCESS_SECRET="change-me-access-secret"
